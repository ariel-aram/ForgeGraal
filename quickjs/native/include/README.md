# Node-API headers

`js_native_api.h`, `js_native_api_types.h`, `node_api.h` and `node_api_types.h` are copied unchanged
from Node.js v26.5.0 (MIT licensed, https://github.com/nodejs/node). They define the Node-API
contract that `../napi.c` implements. Nothing else from Node.js is used.
